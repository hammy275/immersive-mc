package com.hammy275.immersivemc.common.compat.util;

// Since most classes retrieved from CompatUtils are used for an instanceof check,
// we return NullClass when we don't have the class, so we don't need to do null checks.
public class NullClass {
}
